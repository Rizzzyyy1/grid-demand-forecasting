gridcast:
  target: local
  outputs:
    local:
      type: duckdb
      path: "{{ env_var('GRIDCAST_WAREHOUSE', '../data/warehouse.duckdb') }}"
      threads: 4
      settings:
        TimeZone: UTC
